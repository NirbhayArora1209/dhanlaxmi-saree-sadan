import { NextResponse } from 'next/server';

// Placeholder: In a real app, this would use authentication and a database
export async function GET() {
  return NextResponse.json([]); // Empty cart for now
} 